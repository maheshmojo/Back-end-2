pipeline {
    agent any
        tools {
             maven 'Maven-Home'
        }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}