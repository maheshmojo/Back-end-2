pipeline {
    agent any
    tools { 
        maven 'Maven 3.8.1' 
        jdk 'Java-11' 
    }
    stages {
        stage ('Initialize') {
            steps {
                sh '''
                    echo "PATH = ${PATH}"
                    echo "MAVEN-HOME = ${MAVEN-HOME}"
                ''' 
            }
        }

        stage ('Build') {
            steps {
                echo 'This is a minimal pipeline.'
            }
        }
    }
}